<h1> Liste des taches </h1>

<ul style="list-style-type: none">
  <% @tasks.each do |task| %>
  <li>
    <%= task.completed ? "👌" : "👎" %>
    <%= link_to task.title, task_path(task)%>

  </li>
  <% end %>
</ul>

<%= link_to "Creer une nouvelle tache", tasks_new_path %>
